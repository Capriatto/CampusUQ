<!-- Primer commit -->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">
    <!-- Contenido de la actividad -->
    <include layout="@layout/contenido" />

    <!-- Navigation View -->
    <android.support.design.widget.NavigationView
        android:id="@+id/navview"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/encabezado_navview"
        app:menu="@menu/menu_nav"
        />

 <!--
    <fragment
        android:id="@+id/fragmento_lista_noticias"
        class="co.edu.uniquindio.android.electiva.practica_navegation_drawer.Activity.fragment.NoticiasFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
-->
</android.support.v4.widget.DrawerLayout>